# Changelog

## v3.2.0

- 合字に文脈ガードを追加（Monaspace 本家の calt と同じ設計思想）。演算子の並びが
  どの合字よりも長いときは合字化しない: `<|>` `&&=` `~~>` `->>` `==>` は
  素の文字のまま、`->` `-->` `<-->` `>>=` `===` は従来どおり合字を適用。

## v3.1.0

- Light ウェイトを復活（Monaspace の wght 軸下限を下回る分をアウトライン調整で対応）
- リリース版数を name ID 5 / fontRevision に反映
- SCP 未収録の約 600 文字（`ł` `ğ` `ş` `ı` `ř` `₽` …）を半角で取り込み
- `−` `ς` `⁴` などのグリッド外グリフを修正
- 35/Term で半角カナを 1 セル内に中央配置
- Term/35: `pwid`/`palt` を除去し、`hwid` 先のグリフをセルに合わせる
- Nerd Fonts 対応版のアイコンを JP/35 でも 1 セル幅に設定
- 結合文字（U+0300〜036F）を送り幅 0 で取り込み、直前の文字に重なるよう調整
- OS/2 の Unicode / コードページ範囲ビットを最終的な収録文字から再計算
- `makeotc.py` がファミリー内で cmap が一致しない面を検出してエラー停止するように修正
- リリース zip に LICENSE を同梱

## v3.0.0

破壊的変更を含みます。

- ExtraLight/Light 削除（Light は 3.1.0 で復活）
- `= < > | ~` のソースを Monaspace に変更
- Term の曖昧幅を HackGen Console 方式に変更

**既存ユーザーへの影響**: Term で `①` `※` が 2 セル幅になり、曖昧幅を
半角扱いするターミナルでは右隣に食み出る。Windows Terminal では
`"compatibility.ambiguousWidth": "wide"` で 2 セル確保可能。`←` `→` `≠`
`≤` `…` は 1 セル（Monaspace 版）。

- liga 単独で合字が適用されなかったバグを修正
- CI で Term ファミリーの自動検証を開始
