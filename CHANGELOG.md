# Changelog

## v5.0.0

- Nerd Fonts 版は font-patcher と FontForge を使わず、Nerd Fonts が配る
  記号フォント `Symbols Nerd Font Mono`（NerdFontsSymbolsOnly.zip）から
  fontTools で接ぎ木する（`nerdpatch.py`、環境変数 `NF_SYMBOLS`）。
  記号集合は font-patcher の `--complete --mono` と同一（記号フォント自体
  がその出力）。ただし面が既に持っている符号位置は面のもののままにする
  ——本家の既定は上書きで、残すのは `--careful` だが、Powerline を除いた
  重なりは `♥`（U+2665）1 字だけで、これは記号ではなく文字なので
  （`TEXT_OVER_ICON` に固定し、上流が重なりを増やしたら接ぎ木が失敗する）。
  寸法はセル幅 / 記号フォントの em で一律（記号フォント自身のセルより
  広い 2 字だけセルに収めて中央へ）、Powerline の
  範囲は font-patcher 自身の群分けに従う: 区切り（`^xy` の 32 字）はインクを
  セル幅と行の全高に引き伸ばし、記号フォントが付けている食み出しは比率の
  まま残す（進捗バー U+EE00〜EE05 も font-patcher の同じ `^xy` 表にあり、
  中間の 2 字は両端とも食み出すので両側を残す）。ブランチ・鍵などその他
  （`^pa`）は縦横比を保ったまま、セル幅と行の全高のうち先に当たるほうに
  収める。Source Code Pro 自身が持つ Powerline 7 字
  （U+E0A0〜E0A2、E0B0〜E0B3。行より高く描かれている）も記号フォントの
  ものに置き換える（font-patcher と同じ）ので、プロンプトの区切りはすべて
  同じ行ボックスに揃い、セル間・行間に隙間が出ない。接ぎ木後に OS/2 の
  Unicode 範囲ビットを再計算し（私用領域の 1 万字が宣言に載る）、
  name の著作権・デザイナ記録に Nerd Fonts を加える（LICENSE にも
  `Nerd Font Mono` 版の記載を追加）。`verify.py` / `verify_latin.py` は
  Powerline の幾何（区切りがセルと行に届くか、その他が縦横比を保っているか）
  を検査する。CID 構造・STAT・OS/2・post・ヒントは元の
  面のまま残るので、平坦化後の復元処理（`restore_metadata` /
  `patched_bounds` / `fit_nerd_glyphs`）は消えた。1 面 10 秒（従来は
  平坦化 8 秒 + font-patcher 78 秒 + 復元 23 秒）。従来の font-patcher の
  出力はアイコンが 832u 幅（600 セルから食み出す）だったが、接ぎ木では
  すべて 1 セルに収まる。NF の zip に Nerd Fonts の LICENSE を同梱。
  上流の固定タグに `NF_TAG` が加わり、`bump_pins.py` が追随する。CI の
  Regular Upright ジョブは JP 面への接ぎ木も検証する。verify.py は NF 面
  でも幅メタデータを検査する（xAvgCharWidth は接ぎ木後に再計算）

- **基準を英語圏のターミナルフォントに置き換えた**（v5.0.0）。v4.0.0 までは
  Source Han Code JP（SHCJ）が基準だった: 2:3 の比率、SHCJ の行間
  （1.453 em）、SHCJ の `=` バーに Latin の太さを寄せる主従、SHCJ の
  半角/全角の割り当て。Source Code Pro を基準にし、和文がそれに従う:
  - ファミリーは **Sumi Moji JP（3:5、600:1000）** と **Sumi Moji JP Term
    （1:2、600:1200）** の 2 つ。2:3 の旧基本ファミリーと 35 は廃止
    （旧 35 が新しい基本ファミリーに相当）。Term は全角の送りを 2 セルに
    広げるだけの変種になり、Latin・記号・幅の方針は基本ファミリーと共通
  - **行間は Source Code Pro の 984 / −273（1.257 em）**。hhea = typo、
    `USE_TYPO_METRICS`。JetBrains Mono や Cascadia と並べたときの行の高さが
    揃う（SHCJ の行間は 16% 高かった）。win は Source Han Sans の宣言値
  - **ウェイトは Source Code Pro の名前付きインスタンス**: Light 300 /
    Regular 400 / Medium 500 / SemiBold 600 / Bold 700（Normal と Heavy は
    廃止、SemiBold を追加）。Latin は wght を指定してインスタンス化する
    だけ（二分探索なし）、和文は `＝` のバーが Latin の `=` に合う Source
    Han Sans の面を実測で選ぶ（ExtraLight 36u / Normal 63u / Regular 69u /
    Medium 83u / Bold 101u に対し Latin 37 / 62 / 73 / 83 / 104u）。
    Regular の和文は以前の Source Han Sans Regular から Normal に、Bold は
    同じ面のまま。可変フォントの wght 軸は SCP の wght と一致する恒等写像
    になり、軸の範囲は 200〜700
  - **縦組みの字形を接ぎ木に追随させる**（`repoint_features`）。Source Han
    Sans の `vert` / `vrt2` は差し替えた側のグリフから引くので、cmap を
    Sumi Moji のものに向けた 12 字（`「` `、` `…` など、欧文レイヤーが
    取った文字）は縦組みで回転しなくなっていた。`verify.py` は機能の
    有無ではなく実際に字形が変わるかを検査する
  - **Unicode の半角ブロックは 1 セル**（`narrow_halfwidth`）。Source Han
    Sans は半角ハングル字母（U+FFA1〜FFDC）を全角の互換字母と同じグリフに
    割り当てているので、グリッドに乗せると 51 字が 2 桁になっていた
    （v4 の `fit_halfwidth_forms` が守っていた不変条件）。共有グリフの
    1 セル版を作って半角側だけ差し替える。`verify.py` が
    「東アジア文字幅 H はすべて 1 セル」を検査する
  - **グリフをずらしたら GPOS のアンカーも動かす**（`shift_anchors`）。
    `fit_to_grid` と `widen_fullwidth` は新しい送りの中央に字形を寄せる
    ので、アンカー（字形上の点）を置き去りにすると結合記号が元のインクの
    位置に付く。Term で `ㄓ` のインクが 100u 右に動いたのに声調記号
    `˫` が動かず字の上に重なっていた
  - **CID-keyed の `CIDCount` を修復する**: `cffsubr` は charset の最後の
    エントリから `CIDCount` を決めるが、Source Han Sans の CID 空間は疎で
    追加グリフは若い CID を取るため、25,267 と宣言されて 9,749 グリフが
    その外に出ていた（PDF 埋め込みなど CIDCount から CID→GID 表を確保する
    実装では .notdef になる）。`restore_cid_count` が最大 CID + 1 に戻し、
    `verify.py` が検査する
  - **グリッドはフォント全体で守る**: `fit_to_grid` は cmap だけでなく
    全グリフを歩く。`locl` や `ccmp` は既定 ON で、どの符号位置からも
    引けないグリフを画面に出す（Source Han Sans の `⋯` の locl 形は
    1052u で、Term では行が 148u ずれていた）。`verify.py` は「フォント内の
    すべての送りがグリッド上」を検査する
  - **グリッドを崩す GPOS を落とす**: Source Han Sans の `kern` は横組みの
    シェーパで既定 ON で、`あ`+`て` をセルより 20u 詰める。`halt` `palt`
    （横組みの代替メトリクス）ともども削除し、`verify.py` が GPOS と実
    シェーピングの両方で検査する。縦組みの機能（`vert` `vrt2` `vhal`
    `vkrn` `vpal`）は残す（vmtx / vhea を持つので縦組みは従来どおり）
  - **PANOSE の weight を自分の usWeightClass から決める**: 各面は名前が
    同じ Source Han Sans ではなくバーの太さが合う面を使うので、5 面中 4 面
    が 1 段軽い PANOSE を継いでいた（Regular が usWeightClass 400 に対し
    PANOSE 4）。欧文版は Source Code Pro VF の既定値 3 のままだった
  - **Sumi Moji が持つ文字はすべて 1 セル**（ギリシャ・キリル・罫線・矢印
    7 種と `≠ ≤ ≥ …` も、両ファミリーで）。JIS 流の全角字形は `fwid` で
    戻す: 矢印は合字から切り出した全角版、その他は Source Han Sans の
    全角グリフか同フォントの `fwid` 形（`A` → `Ａ`）。`hwid` / `ss09` の
    幅切り替えは廃止。Source Han Sans の比例幅の残り（半角カナ 500、
    Hangul 字母 920、ﬀ、⸻）は、送り幅がいちばん近いグリッド（セルか全角の
    倍数）に中央配置（`fit_to_grid` / `grid_step`）。切り上げではなく
    いちばん近い段に寄せるので、セルを少し超えただけの字が 1 桁余計に
    取ることはなく、ウェイトによって幅が変わることもない。斜体は Source Code Pro Italic に
    ギリシャ・キリルが無いので、この 2 文字体系だけ Source Han Sans の
    比例幅（基準ウェイトで 285〜1005）が残る。どちらも東アジア文字幅は
    曖昧（A）で
    ターミナルは 1 桁しか空けないから、`narrow_letters` が全部 1 セルに
    揃える（インクがセルからはみ出す字だけ、セル −16u に収まるところまで
    詰める。`verify.py` は「ギリシャ・キリルはすべて 1 セル」を検査する）。
    幅は
    ファミリーで 1 回だけ決める（`reference_steps`。送りは Regular のドナー、
    インクは最重量のドナー）ので、ウェイトで太る文字（`Φ` は 757〜850）でも
    Light と Bold で桁数が変わらず、太いウェイトのインクが 1 セルから
    食み出すこともない
  - **Nerd Fonts 版の命名は本家の流儀**: アイコンを 1 セルに収めるので
    ファミリー名 `Sumi Moji JP Nerd Font Mono`、PostScript 名
    `SumiMojiJPNFM-*`（`JetBrainsMono Nerd Font Mono` と同じ）
  - **Source Han Code JP は上流から外れた**（`SHCJ_TTC` / `SHCJ_TAG` 廃止、
    LICENSE の構成要素からも削除）。バーの目標値、半角カナのドナー、
    行間、半角の集合のいずれも不要になった。上流は Source Han Sans /
    Source Code Pro / Monaspace の 3 つ
  - リリースは 2 ファミリー × 5 ウェイト × 2 スタイル = JP 20 面、Sumi
    Moji 静的 10 面（配布は VF 2 本）。faces ジョブは 6 つ
  - `build.py` から `rescale` / `narrow_ambiguous` / `latin_onecell` /
    `shcj_bar_target` が消え、`graft_halfwidth` は Sumi Moji の cmap を
    そのまま載せる。`verify.py` は幅の方針・`fwid`・行間・Source Code Pro
    との `=` バー一致・和文との太さ対応を検査する

## v4.0.0

最後に公開したのは v3.2.0 で、v3.3.0 として書いた下の変更もこのリリースで
初めて出る。

- ファミリー名を Shoyu Code Pro JP から **Sumi Moji JP** に改名
  （PostScript 名 `SumiMojiJP*`、NF は `Sumi Moji JP NF` など、
  ベンダー ID `SUMI`、環境変数 `SHOYU_*` → `SUMI_*`、リリース資産
  `SumiMojiJP.zip` / `SumiMojiJP-NerdFont.zip`）。
  欧文のみの Sumi Moji は仮称を外して正式名に。旧名の面とはファミリー名
  が違うので共存する（置き換えるなら旧版をアンインストール）
- `SumiMoji.zip` は可変フォント 2 面だけに。静的 12 面は JP 面のドナー・
  NF パッチの入力・可変フォントの検証用に組むが配布しない
  （`SumiMoji-NerdFont.zip` は静的面へのパッチのまま）
- TTC を全部廃止（`SumiMojiJP*.ttc` / `SumiMoji.ttc`、`makeotc.py` ごと）。
  リリースの単位を利用の単位（インストールする OTF）に揃える。中身は
  zip の 12 面と同じで、サイズも 3% しか違わなかった
- zip はファミリーごとに（`SumiMojiJP.zip` / `SumiMojiJP35.zip` /
  `SumiMojiJPTerm.zip` と、その `-NerdFont.zip`）。1 回のダウンロードが
  146 MB（NF は 201 MB）から 3 分の 1 になる。1 面のサイズは Source Han
  Sans JP の面（4.6 MB）が床で、JP 面 4.7 MB はそこに着いている
- NF 面は font-patcher のあと cffsubr でサブルーチン化し直す（FontForge
  の書き出しで CFF のサブルーチンが減り、アイコン 1 万個の flat な
  charstring も含めて膨らんでいた。JP Regular の NF 面 7.77 → 6.91 MB、
  1 面 10 秒）
- セルフレビューで見つけた不具合の修正:
  - JP 各面の合字グリフ（61 種 + cv99 の 2 種）の CFF charstring 幅が
    hmtx の送り幅と 510u ずれていた（記号用 FD の nominalWidthX で符号化
    した後に `add_latin_fd` が A の FD 複製へ移していたため。描画は hmtx を
    読むので見た目には出ないが、CFF を読む処理には誤った幅が見えていた）。
    `latin_ligatures` も他の追加処理と同じ A の FD で追加するようにし、
    `verify.py` が全グリフの CFF 幅と hmtx の一致を検査する
  - 600 セルの 35 / Term ファミリーで半角記号 ￨￩￪￫￬￭￮（U+FFE8〜FFEE）の
    送り幅が 500 のままだった（`HALFWIDTH_FORMS` が U+FFDC で止まって
    いた）。East Asian Width "H" の 2 範囲を対象にし、`verify.py` が
    U+FFE9 を検査する
  - `nerdpatch.py`（と当時の `makeotc.py`）の `SumiMoji-*.otf` 探索が VF の
    `SumiMoji-Italic[wght].otf` も拾い、Nerd Fonts パッチが CFF2 の VF を
    受け取る状態だった。静的面だけを列挙する `verifylib.static_faces` に
    置き換え
  - Sumi Moji VF: `head` の外接矩形と `hhea` の広がり（xMaxExtent・両側の
    最小サイドベアリング）が既定マスター（Regular）だけの値だったのを、
    各マスターのアウトラインから測った和にした（保存時の再計算は既定
    インスタンスしか見ないので切る）。`verify_latin_vf.py` は軸の両端と
    既定のインスタンスを丸めなしで描き、外接矩形と hhea の 3 値を
    突き合わせる。GSUB の FeatureParams を付け替えた
    後に元の name レコード（Upright で 73 件）が参照されないまま残って
    いたのと、SCP 由来の STAT / fvar の文字列 5 件を削除（`prune_orphan_names`、
    静的面にも適用）。
    `harmonize_win_metrics` は実行した面だけでなく出力ディレクトリ内の
    ファミリー全面を対象にする（CI は Regular と Light Italic を別ステップ
    で組む）。SCP VF のマスター位置の読み取りを wght 軸に限定
  - `requirements.txt` の fontTools 下限を 4.52.4 に（`cffLib.CFF2ToCFF` と
    `instantiateCFF2(round=)` を使うため。4.50 では import で落ちる）
  - 結合文字の異体（cv11: U+0306 のキリル文字用ブレーヴェ）が送り幅 1 セルの
    グリフとして取り込まれていたため、cv11 を有効にするとアクセントが
    1 セル分の幅を取っていた。既定の結合文字と同じ 0 幅・1 セル左寄せで
    取り込み、GDEF でもマークに分類する。`verify.py` が cv11 の前後で
    U+0306 の送り幅 0 を検査する
  - `_shcj_ref` がプール内で `sys.exit` していたのを例外に（他の面の
    失敗と一緒に報告される）。`verify_latin_vf.py` は wght 軸が無い
    フォントで FAIL を出して終了する
  - **`>>>=` が `>` `>` `≥` になっていた**（JavaScript の符号なし右シフト
    代入）。合字ガードは「合字より長い演算子の連なりは素のまま」を守る
    もので、後ろに伸ばした形自体が合字なら（`>=` の前の `>` は `>>=` に
    なる）ガードを張らない設計だった。ところがその `>>=` は `>>` の
    ガードに先に食われるので発火せず、`>=` だけが無防備に残っていた。
    後読み（バックトラック）を持つガードは長い合字より右から始まるので
    先に長い合字のトリガが一致して連なりを消費する——つまり無条件で
    安全なので、前置ガードだけ無条件にした（後置ガードを無条件にすると
    `===` が素の 3 字になる）。合わせて `=!=` のように末尾がそのまま別の
    合字になっている場合も張るようにし、`==!=` `<===` `<<<=` `<<->`
    `<<-->` `<</>` `<~~>` `==>>` `>>==` が直った。`verify.py` がこの
    10 種を検査する
  - **斜体面の全角矢印 7 種が右にずれていた**。`stretch_arrows` は傾きを
    戻す前の外接矩形で中央合わせしていたが、剪断した図形の外接矩形は
    外接矩形の剪断ではないので、字面の高さ × tan(11°) ぶん右へ寄って
    いた（中心が 67u ずれ、`⇐` は 56u 次のセルに食い込み、`↑` と `↓` の
    中心が 72u 離れていた）。傾きを戻したあとの外接矩形で測るようにし、
    伸縮の目標値からも傾きぶんの広がりを差し引く。`verify.py` が
    7 種すべての中心と送り幅内に収まっていることを検査する
  - Nerd Fonts 版: 記号フォントが自分のセルより広く描いている 2 字
    （U+EE01・U+EE04、2048 のセルに 2252）が、一律縮尺のままだと左右の
    セルに 30u ずつ食い込んでいた。セルに収めて中央に置く。あわせて
    `icon_checks` がアイコン 1 万字ぶんの送り幅・インクの有無・セルへの
    収まりを検査するようにした（それまで測っていたのは Powerline の
    40 字だけで、全アイコンを空にしても 2 セル幅にしても検査は通った）
  - `verify_many.py` はパターンでなく名指しされたフォントが無いときに
    失敗する（リリースの package ジョブが唯一 NF 面を検査する行が、
    黙って飛ばされて「全件成功」と表示されていた）。`golden.py` は
    1 件も比較しなかったときに終了コード 1 を返す。
    `fetch-upstreams` は 4 つの上流すべての探索結果が空でないことを
    確認する（`shs_dir` だけは `dirname ""` が `.` になり、存在するので
    `build.py` の環境変数検査をすり抜けていた）
  - **接ぎ木したグリフの縦組みの原点がずれていた**。`vmtx` の上サイド
    ベアリングは各グリフ自身の yMax から下向きに測るので、ドナーの値を
    そのまま写すと yMax の差だけ原点が動く。追加した 1,529 字すべてが
    `｡`（U+FF61、yMax 243）の 637 を受け取り、原点が 880 のはずのところ
    1,127〜1,293 になっていた。`VORG` を読む処理（HarfBuzz・CoreText・
    DirectWrite）は 880 を使うので影響しないが、`vmtx` を読む処理
    （FreeType の縦組みは `VORG` を読まない）では欧文が 0.25〜0.57 em
    低く、しかも全部同じ値なのでインクの上端が一列に揃ってしまう。
    ドナーの「原点」を受け継ぎ、自分の yMax から引いてベアリングを出す。
    Nerd Fonts 版で差し替える Powerline 7 字も、元より背の高い字形に
    描き替えるので同じ計算をする（U+E0A0 は 804 になっていた）。
    `verify.py` が `vmtx` と `VORG` の一致を全グリフで検査する
  - **進捗バーの記号 6 字（U+EE00〜EE05）が隣のセルに 30u はみ出して
    いた**。font-patcher にはもう 1 つ `^xy` の表（`SYM_ATTR_PROGRESS`）が
    あり、この 6 字は Powerline の区切りと同じく重ねて並べる前提で
    描かれている。Powerline の範囲外なので通常アイコン扱いになり、
    しかも幅は 2048 未満（セル内）なのに位置がセルの外という形なので、
    ラウンド 25 の「幅」での判定を素通りしていた。判定を「インクが
    どこに落ちるか」に変え、6 字を引き伸ばす群に入れ、両端が食み出す
    中間の 2 字は両側の食み出しを残す（font-patcher の align 'c'）
  - `icon_checks` は大きさだけ見て位置を見ていなかったので、全アイコンを
    1 セルぶん横にずらしても、行の遥か上に上げても検査を通った。
    アイコン・区切り・その他すべてを座標で検査する
  - **斜体面のギリシャ・キリルが 1 セルに揃っていなかった**。
    Source Code Pro Italic はこの 2 文字体系を持たないので Source Han
    Sans の比例幅（基準ウェイトで 285〜1005）が残り、`grid_step` は
    いちばん近い段に
    丸めるので `Μ Ж М Ф Ш Щ Ъ Ы Ю ж ф ш щ ю` の 14 字だけ全角に上がって
    いた。どちらも東アジア文字幅は曖昧（A）でターミナルは 1 桁しか
    空けないから、斜体の `МОСКВА` は `М` が `О` に重なり、同じファミリー
    の直立面では正しく出るという状態だった。`narrow_letters` が送り幅を
    1 セルにして中央に置き、インクがセルに収まらない字だけ、セルから
    左右 8u（Source Code Pro が `w` `W` に与えているサイドベアリング）
    空けたところまで詰める。詰めると線が細くなるので、詰める字は必要な
    字だけ——基準ウェイトで 28 字、最重量で 44 字で、残る 87 字はドナー
    のインク幅のまま中央に寄るだけ。欧文ドナー由来のグリフ（直立面の
    ギリシャ・キリルはすべてこれ）は最初から 1 セルなので触らない。
    `verify.py` は例外の固定集合をやめ、「ギリシャ・キリルはすべて
    1 セル」を検査する
  - **Term 面で罫線が繋がっていなかった**。`widen_fullwidth` は全角を
    1000 から 1200 に広げるとき字形を中央に置くので、送り幅いっぱいに
    描かれている字（`＿` `￣` `﹏` `〰` `◢◣◤◥` など 18 字と、`fwid` を
    立てたときの罫線素片・ブロック要素 87 字）はセルの継ぎ目ごとに
    左右 100u ずつ白が入り、`＿` の並びが破線に、`█` の並びが縞に
    なっていた。中央に置くかわりに端を伸ばす（`extend_edges`。中央では
    なく端の 2u 断面を押し出すので、`┼` の縦線は太らない——実測で
    base・Term とも 40u）。`verify.py` が両ファミリー・両幅で継ぎ目が
    無いことを検査する
  - Term 面の罫線の続き: 片側だけ隣に接する字（角 `┌┐└┘`、T字 `├┤`、
    半罫 `╴╶` など 86 字）は前項の「両端に届く」判定に掛からず中央寄せの
    ままで、`┌─┐` の角と罫の間に 100u の隙間が残っていた。届いている側
    だけ押し出し、縦線はセル中央に置いたままにする。また端が水平線で
    ない字（網掛け `░▒▓`、斜線 `╱╲╳`、波線 `〰﹋﹌﹏`、三角 `◢◣◤◥` の
    14 字）は端の断面を押し出すと継ぎ目に横棒や平坦部が出るので、
    アウトライン全体を横に 1.2 倍して連続させる（`edge_is_rule` が
    端 2u と 10u 内側の断面を比べて振り分ける）
  - 前 2 項の判定は「インクが送り幅の端に届いていれば伸ばす」だったので、
    たまたま端に触れているだけの普通の字（`Ⅷ` `㌄` `㌡` `㏂`、Bold では
    漢字 `孰` も）まで Term で 20% 横に伸びていた。また `▏` のような
    端に寄った縦棒は断面が「罫線」と区別できず、押し出されて 125 → 225
    に太り、`▏▎▍▌▋▊▉█` の刻みが揃わなかった。伸ばす対象を符号位置の
    ブロックで明示し（`TILING_STRETCH` / `TILING_RULE`、`fwid` の全角形
    まで追う）、それ以外は従来どおり中央寄せ。ブロック要素・網掛け・
    三角・波線・`＿` `￣` は全体を横 1.2 倍（`▏` は 150 で 8 分の 1 の
    まま）、罫線素片・歯科記号・`√` は端を押し出し、その中でも斜線と
    円弧は伸ばす
  - 破線の罫線 `┄` `┅` `┈` `┉` `╌` `╍` は Source Han Sans が両端の点を
    半間隔ぶん内側に置いて隣のセルと模様が続くように描いているので、
    「端に届いていれば伸ばす」に掛からず Term で中央寄せのままだった
    （セル内の間隔 111u に対し境界で 312u）。伸ばす側に入れ、端に届いて
    いなくても全体を 1.2 倍する。あわせて `verify.py` が Term 面で
    「読み手が到達できる普通の全角字のインクが 1000 を超えない」ことを
    検査する。到達範囲は cmap を出発点に 1 対 1 置換と `aalt` 置換を
    閉包したもの（18,142 グリフ）で、欧文ドナーの 2 セル合字は両ファミリー
    で 1200 幅だが合字ルックアップからしか引けないので入らない。CID 番号
    で絞る最初の版は Source Han Sans の疎な CID 空間のせいで漢字の 6 割を
    見ておらず、`fwid` 1 段だけの次の版は `vert` でしか引けない `｜` や
    `⎰` の縦組み字形を見ていなかった（ラウンド 33 以前の Term Bold は
    実際にその 6 字を伸ばしている）。`Ⅷ` や Bold の `孰` が 20% 伸びた
    2 ラウンドの間、罫線 10 字の検査だけでは通っていた。README のヒント
    付けグリフ数（1,300〜1,800）を実測のおよそ 2,050〜2,550 に直した
  - `verify.py` / `verify_latin.py` のインク位置検査は左右 300u まで
    許すので、4 分の 1 セルのずれは通っていた（漢字全部を 250u 右に
    ずらしても、Term で右端に寄せても合格）。氵のように設計上 278u
    偏る字があるので 1 字ごとの上限は置けず、漢字・かな・欧文の
    インク中心の「平均」が送り幅の中心から 25u 以内にあることを見る
    （実測は漢字 −1.4u、かな +5.6〜+7.3u、欧文 +3〜4u）
  - README の `=` のバー間隔（SCP 152u / Monaspace 197u）と `-` の短さ
    （110u）が v4 の 10/9 拡大時代の数値のままだったのを実測に直した。
    `docs/sumi-moji-plan.md` の Wide 13 字の「両ドナーがそう描いている」
    も README と同じ実測（片方のドナーにしか無い）に揃えた
  - **リリースの「全アセットがある」検査が落ちようがなかった**。
    `zip` は `*.otf` が 1 つも一致しなくても `LICENSE` が一致するので
    終了コード 0 で書庫を作り、`ls -l dist/*.zip` はそれを並べるだけ
    だった。フォントの入っていない zip がそのまま公開されうる。
    6 つのアセットを名前で確かめ、中の `.otf` の数まで数える
  - README の Nerd Fonts の表で `^pa` の行が例示グリフを失って空の
    コードスパンになっていたのを、符号位置で書く形に直した
    （`docs/sumi-moji-plan.md` の同種の 1 箇所も）。東アジア文字幅
    Neutral の全角字数は実測の 142（Unicode 14.0）に直した
  - `verify.py` のインク検査は幅しか見ていなかったので、全部の漢字を
    1 桁ぶん右にずらしたフォントが検査を通った。送り幅の内側に収まって
    いるかを座標で見る。また日本語の収録字数を数えていなかったので、
    cmap を 25,000 字削って大半を `.notdef` にしたフォントも通った。
    下限を検査する——数えるのは cmap ではなくインクを持つグリフなので、
    cmap をそのままに漢字 12,746 字を空にしたフォントも落ちる。
    同じ 2 つの検査を欧文面の `verify_latin.py` にも入れた（そちらは
    cmap の数しか見ていなかったので、1,628 グリフを空にしても、全部を
    1 セルぶん右にずらしても「all checks passed」だった。リリースの
    package ジョブが欧文面と欧文 NF 面を検査する唯一の行がこれ）。
    可変フォント側の `verify_latin_vf.py` にも、静的面が持っていた
    3 つの検査（収録字数・送り幅がグリッドに乗っているか・GSUB の
    機能一式）を入れた。可変 2 面は `SumiMoji.zip` の中身そのもので、
    この検査が唯一の関門だが、U+024F より上の 968 字を全部落としても、
    `ss01`〜`ss08` `cv99` `zero` `salt` を全部落としても通っていた
  - `icon_checks` はアイコンを片側からしか押さえていなかったので、
    1 万字すべてを半分の大きさに描き直しても、左右反転しても検査を
    通った。接ぎ木元の記号グリフと `icon_transform` から出る箱を全
    10,409 字ぶん突き合わせる検査を足した（半分に縮めた 399 字を
    実際に検出することを確認済み）。`NF_SYMBOLS` が無いときは
    「測っていない」と表示する（それまでは測れないまま `0` と出て、
    読む側には合格に見えていた）
  - LICENSE の記載を実物に合わせた: Source Han Sans の著作権表示を
    2014-2025 に、Source Code Pro を 2023 に（いずれも固定した上流の
    宣言値）、Monaspace の範囲を「合字のみ」から「合字・単独 ASCII 記号・
    そこから切り出した全角矢印」に。README と `verify.py` の斜体全角
    14 字の実測値も直した（送り幅 755〜1005、最重量のインク 672〜972。
    同じ数値が CHANGELOG の v5.0.0 本文にも残っていた）。README の
    Nerd Fonts 節が「同じ相対寸法」と書いていたのを実際（同じ記号集合・
    同じ 1 セル送り。寸法差は `docs/sumi-moji-plan.md`）に、`^pa` の行を
    「行の全高いっぱい」から「セル幅と行の全高のうち先に当たるほう」に、
    東アジア文字幅 Wide の 13 字の「両ドナーが 1 セルで描いている」を
    実測（どれも片方のドナーにしかなく、声調記号 2 字は 250）に直した。
    CONTRIBUTING が指していた「Pin upstream releases」ステップに実際に
    その名前を付けた
  - **欧文ドナーの `ccmp` が接ぎ木を越えていなかった**。`ccmp` は
    どのシェーパーでも既定でオンの合成・分解機能だが、
    `import_scp_variants` は異体字の機能（`zero` `salt` `cvNN` `ssNN`）
    しか載せず、JP 面には Source Han Sans の `ccmp` しか残っていなかった
    ——Source Code Pro のグリフを何も知らない側だけが。その結果
    `i` + U+0307 は自分の点を残したまま 84u ずれた位置にもう 1 つ点を
    描き、`j` は 10 種のアクセントと衝突し、`g̃`、ベトナム語の `ê̆` `ô̆`、
    `Ї́` `ї́` は合成されなかった（欧文単独面と食い違う 20 系列）。
    `import_scp_ccmp` が、機能の描くグリフのうち接ぎ木に無いもの
    （合成済みの結合記号、点のない `ı` `ȷ` 系、積み重ね用の平たい
    アクセント。正体 62・斜体 46 字）を `graft_halfwidth` と同じ扱いで
    足し、ルックアップをグリフ名と入れ子ルックアップ番号ごと書き換えて
    ドナー自身の順序で全 `ccmp` フィーチャレコードに入れる。
    フィーチャに載せるのはドナーのフィーチャが挙げていたものだけ
    （連鎖文脈が呼ぶ側も一緒に載せた最初の版では、文脈を捨てたまま
    無条件に走って、地の文の `i` `j` がすべて点なしになり、小文字の
    上のアクセントが大文字用の高さに 129u 跳ね上がっていた）
  - **全角の罫線が 1 行ぶんの高さに足りていなかった**。行は 1257u
    （Source Code Pro の 984 / −273）で Source Han Sans は 1000u の
    em に描くので、`fwid` を立てると `│` の縦一列は行ごとに切れ、`█` の
    並びは 1257 のうち 257 が白い縞になった。1 セルの既定形は欧文ドナーが
    −400..1000 に描くので出ていなかった問題。`tile_vertically` が全角側を
    同じ帯に合わせる（1 面あたり 145 字）。縦の分類は横のものを転置した
    ものではない（`VTILING_RULE` / `VTILING_SCALE`）。罫線は押し出す
    ——インクが em の端に届き、そこが罫線になっている場合だけ、
    `widen_fullwidth` が横にする 2 つの判定を字形を転置して行うので、
    縦線の太さは変わらない。ブロック要素と縦の破線は em を帯に写す
    ——`▁` は行の 8 分の 1、`▀` は半分のまま（押し出した最初の版では
    どれも同じだけ伸びて `▁` が 29% になり、8 分の 1 刻みが崩れていた）。
    網かけ `░▒▓` はどちらでもない。全角形を持たない字（`＿` `￣` `〰`
    `◢`）は縦には積まないので触らない（最初の版は `＿` の 41u の罫線を
    320u の帯にしていた）
  - **`⸺` `⸻` が、繋がるべきところで切れていた**。Source Han Sans は
    2 倍ダッシュを送り幅 1672 の中に 1580 のインクで描く（継ぎ目 92u）。
    グリッドの段は 2000 で、そこに中央寄せすると継ぎ目は base で 420、
    Term で 820 になっていた（3 倍ダッシュは 640 と 1240）。タイリング
    する字として扱い、`fit_to_grid` が中央寄せでなく段いっぱいに伸ばす
    ので、継ぎ目は 110 と 132 になる。`verify.py` は縦の継ぎ目・ダッシュの
    継ぎ目・`ccmp` の合成の 3 つを検査し、さらに「`ccmp` がドナーの文脈
    でしか発火しない」「`＿` `￣` が帯でなく罫線のまま」「`fwid` の
    ブロック要素が行の 8 分の 1 刻み」の 3 つを検査する（6 つとも
    変異フォントで落ちることを確認済み）
  - **欧文ドナーの GPOS マーク位置決めが接ぎ木を越えていなかった**。
    Source Code Pro は結合記号をスペーシング字形として描き、位置決めを
    まるごと GPOS に置いている（`mark` は字のトップアンカーに掛けるので、
    上伸部では x ハイトより 229u 高い）。`graft_halfwidth` の
    「送り幅 0 ＋ インクを 1 セル左」は x ハイトの字には合うが、それ以外は
    合わない——`b d f h k l` の上伸部とアクセントは 84 組中 58 組で
    インクが重なっていた（欧文単独面は 0 組）。`import_scp_marks` が
    ドナーの `mark` / `mkmk` ルックアップを写す（グリフ名を書き換え、
    カバレッジは隣のアンカー配列ごと並べ替え——マークのカバレッジは
    集合ではなく順序を持つ——、マーク側のアンカーは字形と同じだけ
    1 セル左へ）。GDEF のマーク結合クラスも、それを読むルックアップと
    一緒に渡す
  - **写した `ccmp` がルックアップ列の末尾に置かれ、異体字機能より後に
    走っていた**。シェーパーはフィーチャの並び順ではなくルックアップ列の
    順に適用するので、`cv04` / `ss14` / `salt` を立てるとセリフ付きの `i` が
    点を持ったまま結合記号に出会い、28u ずれた位置にもう 1 つ点を描いて
    いた。先頭に挿入し、既存の参照をすべて繰り上げる。あわせて `ccmp` の
    グリフ接ぎ木は 2 回走らせる（異体字機能には `ccmp` が合成した字への
    規則があり——`cv02` の 1 階建て `g̃`——、`ccmp` には異体字が描く字への
    規則がある——`cv04` のセリフ付き `i` に付くオゴネク）
  - **丸角 `╭ ╮ ╯ ╰` が縦にも横にも処理から漏れていた**。脚は
    `edge_is_rule` が見る位置ですでに曲がり始めていて、実測 0.0059 は
    許容値 0.005 を超える（全ウェイトで同値）。そのため行ごとに 137u
    足りず `┌ ┐ ┘ └` だけが繋がり、Term では押し出しでなく伸縮に回って
    軸が 48u（他の `fwid` 縦線は 40u）になっていた。許容値を 0.007 に
    ——罫線ブロックが弾くべきもの（斜線 0.34）からはなお遠い。
    斜線 `╱ ╲ ╳` は横と同じく全体を写す側に入れた（押し出すと尾が伸びる。
    1 セルの既定形はもともと 600 のセルに 1200 の高さで描かれている）
  - **網かけ `░ ▒ ▓` が 1400u の行に対して 400u 足りなかった**。伸ばすと
    点が楕円になるので、模様を em ぶん上下に繰り返して帯で切る
    ——上下のセルが見せていたはずのものをそのまま描く
  - 上の 4 つに対応して `verify.py` は「アクセントが字を避ける」
    「丸角が角の箱を保つ」「1 セル既定形が行の端に届く字は全角形も届く」
    「異体字機能が `ccmp` の合成字に届く」を検査する（4 つとも変異
    フォントで落ちることを確認済み）。`tile_vertically` の 3 つのガードと
    クラスの優先順位、`_insert_lookups_first` の採番にもテストを足した
  - **二重にまたがる結合記号 `͟` `͡` が 1 字目の上に乗っていた**。
    ドナーはこの 2 字だけ GPOS で半セル左に引いている（`XPlacement`
    −300）のに、隣の「送り幅を 1 セル減らす」指定ごと捨てていた。
    我々の字形はすでに 1 セル左にあるので、必要な指定はドナーより
    1 セルぶん右（+300）。あわせてドナーの GPOS `ccmp`（上伸部では
    `͡` を +80、下伸部では `͟` を −140）も移す——クラス方式の連鎖文脈
    （Format 2）を含む。欧文単独面と `a͡b` `g͟j` `b͡d` `p͡q` が完全一致する
  - **Term 面で全角の結合記号が 1000 のセルに取り残されていた**。
    `widen_fullwidth` は送り幅 0 のグリフを飛ばすので、囲み記号
    `⃝` `⃞`、濁点・半濁点、声調記号は 1000 幅のセルに置かれたまま、
    土台だけが 1200 の中で 100u 右に動いていた——`国⃝` は円が漢字の
    左端を横切っていた。これらは原点の 1 全角ぶん「左」に描かれている
    （我々が欧文の結合記号を 1 セル左に描くのと同じ）ので、セルが
    広がったぶん逆向きに 100u 動かす。`国⃝` `一⃝` とも同心に戻った
  - `verify.py` は「GPOS が `mark` / `mkmk` / `ccmp` を持つ」
    「2 つ目のアクセントが 1 つ目から浮く」「GDEF が GPOS の絞り込む
    マーククラスを持つ」「注音の声調記号が字のインクに付く（セルでは
    なく）」「`͡` が繋ぐ 2 字にまたがる」「囲み記号が字を囲んだまま」
    「カバレッジがグリフ ID 順」を検査する。7 つとも、ビルドを 6 通りに
    壊して焼き直した面で落ちることを確認済み（`mkmk` を渡さない／GDEF の
    クラスを渡さない／`shift_anchors` を外す／`XPlacement` を捨てる／
    全角結合記号を動かさない／カバレッジを並べ替えない）
  - **`fwid` の縦破線が行ごとに繋ぎ目を崩していた**。`tile_vertically` は
    全角形を 1 セル既定形の帯（−400..1000 の 1400u）に写すが、模様が
    繰り返す間隔は帯ではなく行送り 1257u。11% 長くなるので `┊` は
    行境界で 57u 重なって 264u の破線の中に 471u の 1 本ができ、`┆` は
    セル内 156u に対し境界 13u だった。縦破線だけは em を行の箱に写す
    （`VTILING_PERIOD`）
  - **Term で全角結合記号を動かしたぶん、半角の土台の上でずれていた**。
    ラウンド 39 の −100 は全角のセルが広がったときだけ正しく、半角層
    （欧文と半角カタカナ）は両ファミリーとも 1 セルのまま。`ｶ` `ﾈ` `ｳ`
    に濁点・半濁点を付けた 116 組のうち 25 組が、触れていなかった状態から
    最大 4,651 平方ユニット重なっていた。直前のグリフが 1 セル幅のときは
    GPOS の文脈規則で 100u を戻す（`realign_halfwidth_marks`）
  - **欧文ドナーの `locl` が移っていなかった**。ギリシャ文字には
    `Β` + U+0301 でラテンの大文字用アキュート（188u 幅）が出ていた
    ——ドナーはトノス（132u）を当てる——うえ、SCP 自身の `ccmp` は
    `locl` の出力から気息記号を合成するので、`ρ` + U+0313 + U+0301 は
    合成されずプシリがアキュートの中に描かれていた（192 系列）。
    `import_scp_locl` が写す。`locl` は「どこでも同じルックアップ」では
    ないので、登録はドナーと同じ（スクリプト・言語）の組ごとに行う
    ——union を全部に付けた最初の版はキリル文字にギリシャのトノスを
    持ち込み、`ї` + U+0301 が合成されなくなった
  - 上の 3 つと、レビューで見つかった検査の穴に対応して `verify.py` は
    「破線が継ぎ目で模様を保つ（縦横とも）」「横方向のブロック要素も
    8 分の 1 刻み」「ギリシャ文字がギリシャのアクセントを取り、気息記号を
    合成する」「濁点が半角カナを避ける」「2 つ目のアクセントが 1 つ目より
    下がらない（12/13 以上）」「フォント名・PostScript 名・nameID 0 の
    出典表記がある」を検査する。いずれも、ビルドを壊して焼き直した面か
    変異フォントで落ちることを確認済み。リリースの上流キャッシュ鍵に
    `SCP_TAG` を足し（`-vf` 以外が動いた版で古いキャッシュを引いていた）、
    CHANGELOG に該当節が無いまま公開しないようリリースノートの
    空チェックを足した
  - **Term で縦組みの全角結合記号が列の中心から 100u ずれていた**。
    Source Han Sans はこれらを `vert` の配置値（`XPlacement` 500）で
    列に合わせている——アンカーと同じく字形に対して測った値——ので、
    字形を動かしたぶん配置値も動かす必要があった（`shift_mark_placements`）。
    囲み記号が列の左に寄り、`U+302A` `U+302B` は列の左端の外に、
    `U+302C` `U+302D` は内側に入っていた
  - **同じく Term で、結合記号が 2 つ以上続くと 2 つ目以降が戻らなかった**。
    半角の土台の上で −100 を戻す文脈規則は直前のグリフを見るが、
    結合記号の送り幅は 0 なので `ｶ` `゛` `⃝` の `⃝` の手前は仮名ではなく
    濁点だった。深さ 3 までのサブテーブルを持たせる
  - **`locl` のセルビア語・北部サーミ語の字形が写されたまま到達不能
    だった**。Source Han Sans JP にはその言語の LangSys が無いので、
    セルビア語の `б` とサーミ語の `Ŋ` はフォントの中にあって引けなかった。
    無ければ作る（`_new_langsys`）。また Source Han Sans は 1 つの `locl`
    レコードをスクリプト既定と言語で共有しているので、そこに追記すると
    セルビア語の `б` がキリル文字すべてに出た——`_add_feature_where` は
    既存のレコードを書き換えず、必要な組み合わせごとに新しいレコードを
    足して差し替える
  - **欧文イタリック 5 面（と可変イタリック）で、U+0310 の後ろの
    結合記号が軒並み外れていた**。Source Code Pro の `ccmp` は大文字の
    後ろで U+0310 を `.cap` 字形に差し替えるが、イタリック側のドナーは
    その字形を GDEF で分類していない。`GlyphClassDef` を持つフォントで
    は、置換されたグリフのクラスは GDEF だけが決める（Unicode の
    一般カテゴリへのフォールバックは無い）ので `.cap` は「土台」に
    なり、次のマークの mark-to-base 探索がそこで止まって諦める——
    `E` + U+0310 + U+0301 は 2 つとも次の文字の上に乗り、`E` を土台に
    した 53 個の結合記号のうち 23 個が付かなくなっていた。
    `classify_unicode_marks` は cmap にある Mn しか見ていなかった
    ので、機能からしか届かない字形を取りこぼす。GSUB の単一・複数・
    交替の置換をたどって推移閉包を取る（合成はたどらない——構成要素が
    全部マークとは限らない）。なお U+0310 自身がイタリックで次の
    セルに出るのは上流 Source Code Pro Italic がその字にアンカーを
    持たないためで（U+031A U+031B U+0334 U+0344 も同じ）、こちらの
    問題ではない
  - **注音符号の声調記号が字の上に重なっていた**（全ファミリー・
    全ウェイト。Term 化の問題ではなく基本ファミリーにもある）。
    Source Han Sans は注音字母に声調記号を 3 つの MarkBasePos で
    付けているが、そのマークカバレッジが指すのは SHS 自身の
    U+0300 U+0301 U+0307 U+030C。欧文の接ぎ木がこの 4 つの符号位置を
    ドナーのアクセントに差し替えるので、カバレッジはどの符号位置からも
    届かないグリフを指したままになり、何も付かなくなっていた——
    172 組中 139 組で記号が字の画とインクを共有する（`ㄓ́` は
    636..836、字は 66..934。上流は右肩の 760..1120 に掛ける）。
    差し替えられたグリフのカバレッジにこちらのグリフを足す
    （`rehome_replaced_marks`）。字形が違うので、アンカーは 2 つの
    インクの重心の差だけずらして移す——上流とインクの重心が完全に
    一致する。U+02EA U+02EB は接ぎ木が触っていないので元から付いて
    いて、`verify.py` が見ていたのはその 2 つだけだった。
    なお修正後も 58 組はインクを共有する——Source Han Sans 自身が
    アンカーを持たない組で、上流のアクセントは次のセルに落ち、
    1 セル左に描く我々のは字に重なる。これは接ぎ木の約束事そのもの
    （`日` や `あ` にアクセントを付けても同じ）で、この修正の範囲では
    ない
  - **Term の Nerd Fonts 版で、1 万 402 個のアイコンの後ろの全角
    結合記号が 100u ずれていた**。`realign_halfwidth_marks` の後読みは
    「`widen_fullwidth` が動かさなかったグリフ」を広げた時点で
    固定されるが、`nerdpatch.py` は出来上がった面にアイコンを
    足す。`` + `⃝` は −465..465、同じ 1 セルの字の後ろなら
    −365..565。接ぎ木のあとに後読みを広げる
    （`extend_realign_bases`）
  - **欧文静的 10 面のうち 8 面が、CFF Private の整列ゾーンと
    ステム幅を非整数で出荷していた**。可変フォントを実体化すると
    ゾーンの端がそれぞれ補間され、`round_outlines` と
    `fix_zone_order` はアウトラインと並び順は直していたが値そのものは
    丸めていなかった——`SumiMoji-Bold` と `SumiMoji-BoldItalic` は
    BlueValues 9 個ほか計 12 個（`733.9999999` `671.9999999`、
    `StdHW 115.33964`）、Light と Light Italic は 10 個ほか計 14 個。CFF の仕様では整数の差分で、
    切り捨てる実装は 1u 低く読む（ゾーンが抑えるはずのオーバー
    シュートの下に入る）。Regular と Regular Italic だけが整数
    だったのは Source Code Pro 自身の既定マスターに乗っているため。
    JP 面は `latin_blue_zones` が測り直して丸めているので無事だった
  - **Term の Medium・SemiBold・Bold で、全角の結合記号が半分以上
    まったく動いていなかった**。`fullwidth_marks` は「全角のセルの中に
    描かれている」を上下 2 ユニットの余裕で見ていたが、Source Han Sans
    の線は太さとともに外へ出る——Medium では声調記号 `〪` `〫` が
    −1007、`〬` `〭` が +7、Bold では濁点・半濁点も +5 / +7 に達する。
    8 字のうち Medium で 4 字、Bold で 6 字が集合から外れ、1000 の
    セルに取り残されていた（5 ウェイト中 3 つで囲み記号が字から 100u
    ずれる）。しかも欠けた集合が `shift_mark_placements` の
    「同じ配置を共有するグリフが動くものと動かないものに割れた」に
    当たって**ビルドが止まっていた**——CI のリリース試走はこれで
    ラウンド 42 から赤かった（ローカルでは Regular しか焼いて
    いなかった）。判定を「インクの重心が原点より左」に変える。ドナーの
    欧文結合記号は原点の右に描かれ、いちばん広い U+0304 でも原点中心
    なので、どのウェイトでも取り違えない。あわせて
    `shift_mark_placements` は Format 2（グリフごとに値を持つ）の
    サブテーブルなら該当グリフだけを動かす
  - 検査の 3 つの境界値が Regular でだけ測られていた。**上流と完全に
    一致する面が落ちていた**: 半角カナと濁点の重なり（上流 Source Han
    Sans Bold 自身が `ﾈ゙` で 2,844、`ﾈ゚` で 4,647 平方ユニット重ねる。
    我々は最大 176——半角カナを 1 セルに広げているぶん離れる——で、
    捕まえたい退行は 4,651 だったので上限をセルの 1/400 にする）、
    縦組みの列からのはみ出し（上流 Bold 自身が `〬` を 306..513 に置く。
    ±500 ちょうどではなく全角の 1/20 の余裕を見る。退行は 100u）、
    積み重ねアクセント（`probes - 2` という定数だったが、ドナーが
    「丸い印の上の平たい印」を動かさない回数はウェイトと斜体で変わる
    ——Source Code Pro Italic は wght 400 で 13 組中 11、700 で 9、
    直立は 12。ドナー自身を同じウェイトで測って比べる）、囲み記号の
    中心ずれ（上流 Source Han Sans の `一` 自身が ExtraLight で
    セルの中心から 2.5u 左にある——輪が 40..960、字が 50..955 で
    上流と完全一致——ので 2u ではなく 5u。退行は 100u）。
    5 ウェイト × 2 スタイル × 2 ファミリーの 20 面と欧文 10 面、
    計 30 面すべてで検査が通ることを確認した
  - **Term で、多セルの合字の後ろに付いた全角結合記号が補正されて
    いなかった**。文脈規則の後読みが「送り幅 1 セル」を条件にしていたが、
    欧文層には `==`（1200）`===` `!==`（1800）など 63 字の多セル合字が
    あり、これらは `widen_fullwidth` の `skip` に入っていて両ファミリー
    とも同じ送り幅——つまり「広がっていない土台」なのに 1 セルではない。
    61 の合字 × 8 字の結合記号 = 488 組すべてで囲み記号が自分のセルより
    100u 左に出ていた（`＝＝⃝` の円が 135..1065、基本ファミリーでは
    235..1165）。送り幅では区別できない（Term では広げた全角も 1200）
    ので、条件を「`widen_fullwidth` が実際に動かさなかったグリフ」に
    変える。`verify.py` は「全角結合記号が多セル合字の後ろでも 1 字の
    後ろと同じ位置に付く」を検査する
  - **Term で、間に別の結合記号が挟まると全角結合記号の補正が効かなく
    なっていた**。文脈規則の後読みが「動かした 8 字」しか認めなかったので、
    `B` + U+0300 + `⃝` のように欧文のアクセントが挟まる 1,908 系列で
    囲み記号が 100u 左にずれていた。ルックアップに自前のマーク結合
    クラスを与えて「ほかの結合記号は読み飛ばす」ようにする——挟まるのが
    我々の 8 字自身のときだけ深さで数える
  - **Term の縦組みで、半角の字の後ろの全角結合記号が列から 100u
    はみ出していた**。横組み用の補正を `mark` に載せていたが、シェーパーは
    `mark` を縦組みでも走らせる——そちらは Source Han Sans 自身の `vert`
    の配置で列に載っているので二重補正になり、声調記号 `〬` `〭` が列の
    外に出ていた。水平専用の `dist` に移す。あわせて連続する結合記号の
    深さを 3 まで見る（4 つ目は戻っていなかった）
  - **`cv11` のブレーヴェが二重に接ぎ木され、機能が選ぶほうにアンカーが
    無かった**。同じドナーグリフを `locl` の接ぎ木と
    `import_scp_variants` がそれぞれ作っていたため、`import_scp_marks`
    は前者にだけアンカーを移し、`cv11` を立てると上伸部の下 229u に
    落ちた——127 字でずれ、35 字では字のアウトラインと重なった。
    すでに接ぎ木済みなら使い回す（`cv06` の `Ŋ` も同じ理由で直る）
  - 検査の穴を埋める。**合字グリフがインクを持つかを誰も見ていなかった**
    ——61 字のうち 50 字を空にした JP 面も、61 字すべてを空にした欧文面も
    「all checks passed」だった（`!=` が空白で出る）。**可変フォントは
    1,632 グリフ中 1,626 を空にしても通った**（収録数の検査が cmap の
    数を数えていたため。可変 2 面は `SumiMoji.zip` そのもので、
    `verify_latin_vf.py` が唯一の関門）。**JP 面にはヒントの検査が
    無かった**——ヒント付けを丸ごと飛ばした面が 193/193 で通る。
    **欧文面と可変フォントはイタリック／ボールドのビットを一切見て
    いなかった**（`fsSelection` と `macStyle` のイタリックを落とし
    `post.italicAngle` を 0 にしても通る。Windows のファミリー連結は
    この名前ではなくビットを読む）。**cmap は format 12 しか読んで
    いなかった**——GDI 系が読む format 4 の 2 つを丸ごと消しても、
    U+2000 未満を全部「日」に向けても通った。ほかに OS/2 のコード
    ページ範囲・`fsType`・`achVendID`・`usWeightClass`・nameID 16/17・
    `post` の下線・`vhea` の 4 つの extent・バージョン表記（`head`
    `fontRevision` と nameID 3/5 を `SUMI_VERSION` と突き合わせる。
    ローカルの `dist/` は JP が 3.3.0、欧文が 5.0.0 で揃っていなかった）。
    いずれも変異フォントで落ちることを確認済み。また `y_rows` が空の
    グリフで `min()` を呼んで検査を中断していたのを、報告して続ける形に
    直した（「どの検査も他を止めない」という同ファイルの約束どおりに）。
    上の 2 件にも検査を足した——「半角の字の後ろでも結合記号が縦の列に
    収まる」「位置決めされている記号の異体字も位置決めされている」
    （どちらもビルドを壊して焼き直した面で落ちることを確認済み）
  - さらに検査の穴を埋める。**可変フォントはバージョン表記を見て
    いなかった**——`SumiMoji.zip` の中身そのものなのに、ローカルの
    `dist/` は可変 2 面だけ 3.3.0 のままで通っていた。リリースの
    package ジョブも `SUMI_VERSION` を設定していなかったので、
    組み上げた `dist/` の検査がバージョンを見ないまま走っていた。
    **欧文面は `verify.py` が持つテーブル検査を何も持っていなかった**
    ——`head` の外接矩形・`hhea` の extent・`fsType`・`achVendID`・
    `post` の下線・OS/2 の範囲ビット・format 4 の cmap をすべて壊しても
    通った（4 セルの合字が宣言された外接矩形の外に出る）。共通化して
    両方から呼ぶ。**`STAT` を読む検査が無かった**（消しても ok の数すら
    変わらない）。**縦組み用テーブルの検査が `if` で囲まれていた**
    ——`vhea` `vmtx` `VORG` を消すと 5 つの検査が黙って消えるだけだった。
    **欧文用 FontDict の整列ゾーンを読む検査が無かった**——Source Han
    Sans の 540/733 に差し替えても通る（実際の x ハイト 486 / キャップ
    656）。**異体字セレクタの cmap（format 14、17 セレクタ・1,383
    グリフ）は検査から除外されていた**——Term の「伸びていない」検査の
    到達範囲にも入れた（18,166 → 18,889 グリフ）。`nerdpatch.py` と
    計画書の「縦長のアイコン 3,260 字」を実測の 3,496 字に直した
  - `verify.py` は「`vrt2` も回転字形に届く」「保持するはずの機能
    （`fwid` `hwid` `aalt` `dlig` `ruby` `jp78` `jp83` `jp90` `nlck`
    `locl` `ccmp` `vkrn` `vhal` `vpal`）がある」「罫線素片・ブロック
    160 字とその全角形がインクを持つ」「`head` の外接矩形と `hhea` の
    各 extent が実際のアウトラインと一致する」「`fsType` が 0」
    「OS/2 の Unicode 範囲ビットが cmap と一致する」「囲み記号が縦組みでも
    字を囲む」「結合記号の位置が手前の結合記号の数に依らない」
    「ドナーの言語別字形が引ける」を検査する。`harmonize_latin.py` は
    対象の面が 1 つも無ければ止まる（リリースの package ジョブで欧文
    ファミリーの win メトリクスを揃える唯一のパスで、出力は誰も見て
    いなかった）。README と計画書のイタリック角（−12° と 1° のシアー
    ではなく −11° でシアー無し）とヒント付けグリフ数（2,250〜2,650）を
    実測に直した
  - なお検査の穴を埋める。**可変フォント 2 面は「どこにインクがあるか」
    を一切見ていなかった**——見ていたのは既定ウェイトで「描画される」
    ことだけで、軸の端を丸ごと 1 セルずらしたマスターでも通る（点の
    対応は取れているので varLib は素直に合成し、`head` / `hhea` は
    同じマスターから計算されるので矛盾も出ず、Source Code Pro との
    一致検査はドナー側の字だけを見る）。実際にビルドを壊して最重量
    マスターの Monaspace 合字だけを +600u 動かすと、Bold の `!=` が
    隣の桁に描かれるのに「all checks passed」だった。軸の最小・既定・
    最大の 3 点で「送り幅の中にインクがある」「文字の重心がセルの中央」
    を見る。**可変フォントはテーブル検査も呼んでいなかった**
    ——`fsType` を 8（埋め込み禁止）にしても、`achVendID` を潰しても、
    win メトリクスを 0 にしても、format 4 の cmap を両方消しても、
    `GPOS` や `GDEF` を丸ごと消しても通った。`build_latin.fit_win_metrics`
    の呼び出しを 1 行消すと 984/273（実際の箱は 1060/−454）で組み上がり、
    GDI が上 76u・下 181u を切る面が検査を素通りする。**名前付き
    インスタンスの「名前」を誰も見ていなかった**——メニューに
    「Weight 300」と並ぶ面が通り、しかも対応する静的フォントとの比較
    （このファイル唯一のウェイト別比較）が黙って skip されていた。
    package ジョブでは 10 面すべて揃っているので、skip は「対象外」
    ではなく「検査を失った」の意味。既定インスタンスの nameID 6 も、
    `STAT` の `ital` の値（直立の面が「Italic」を名乗っていても通った）
    も見ていなかった。**静的な面の `STAT` の中身はどちらのファミリーでも
    未検査**だった——`build.add_stat` が面ごとに自分のウェイトだけを
    書くのはそのためなのに、Regular の値を 900 に書き換えても、`ital`
    の値を落としても通る。**欧文面は `usWeightClass` を自分の
    サブファミリーと突き合わせていなかった**（PANOSE のほうを
    `usWeightClass` から導いていたので、いくつでも自己無矛盾だった）。
    **欧文面と可変フォントは `GDEF` を読んでいなかった**
    ——`build.classify_unicode_marks` の呼び出しを消すと `͟` `͡` が
    マークでなくなるが、ビルドも検査も単体テストも通る。ほかに OS/2 の
    バージョン・WWS ビット・`usWidthClass`・`head` の `unitsPerEm`、
    欧文面の nameID 一式も未検査だった。いずれも変異フォント 17 通りと
    ビルド改変 2 通りで落ちることを確認済み。**なお JP 面の `GDEF`
    マーククラスはこの対象外**——Source Han Sans 自身が 9 字
    （U+0304 U+20DD U+20DE U+302A〜302D U+3099 U+309A）を分類して
    おらず、分類しても何も変わらない（JP 面には `IgnoreMarks` も
    マーク絞り込みも無く、測った系列はすべて同一に整形される）
  - さらに検査の穴を埋める。**可変フォントは 4 つあるマスターのうち
    3 点でしか測っていなかった**——軸の最小・既定・最大に加えて
    Monaspace の下限マスター（`mona_floor_wght`、直立 364.75 /
    斜体 381.15。名前付きインスタンスがどれも乗っていない）がある。
    区分線形の補間が折れるのはマスターの位置だけなので、そこは
    まさに壊れが隠れる場所で、実際に下限マスターでだけ Monaspace の
    合字を 600u 動かしたビルドは `->` を自分の送り幅から 535u
    外に出しながら「all checks passed」だった。マスターの位置は
    CFF2 の VarStore の領域ピークから avar と fvar を逆に辿って
    フォント自身から読む。**カバレッジがグリフ ID 順かを欧文面と
    可変フォントは見ていなかった**——マークカバレッジ 7 つを
    逆順にすると `k` + U+0308 の輪が 229 から 60 に落ちて字を貫くが、
    どちらも合格していた。**CFF の Private を読む検査がどこにも
    無かった**（上の 1 件がそれで出荷されていた）。**JP 面は `DSIG`
    を禁じていなかった**——Source Han Sans が持っており、ビルドが
    外すのをやめたら中身と合わない署名を出荷する。欧文静的面の
    コードページ範囲も見るようにした。いずれも変異フォントと
    ビルド改変で落ちることを確認済み
  - またさらに検査の穴を埋める。**欧文面と可変フォントはマークの
    アンカーを誰も読んでいなかった**——`MarkBasePos` の土台アンカー
    502 個を y=0 にすると `b̀` `h́` `l̈` `x̀` ……どのアクセントも字を
    貫くが、両方とも「all checks passed」だった。欧文面こそ
    Source Code Pro のマーク GPOS が本来ある場所で、JP 面はそれを
    取り込んでいるだけなのに、検査は JP 面にしかなかった。
    **OS/2 の `sxHeight` `sCapHeight` `xAvgCharWidth` も同様**
    （`set_latin_heights` が 486/656 を書くのに、Source Han Sans の
    543/733 を入れても通る——CSS の `font-size-adjust` や、キャップ
    ハイトを基準にアイコンを並べる端末が 12% ずれる）。**整列ゾーンが
    その面自身の x ハイト・キャップかも見ていなかった**
    （`BlueValues` を上流の 540/733 にしても、全部空にしても通る）。
    **可変フォントは nameID 3/4/8/9/11/13/14 を見ていなかった**
    （13 と 14 はライセンスとその URL）。注音の新しい検査も
    「付いていて重ならない」だけで位置の上限が無く、アンカーを
    1 セルずらして隣のセルに放り出しても通っていたので、
    「字の右肩から全角の半分以内」を足した。いずれも変異フォントで
    落ちることを確認済み。あわせて `nerdpatch.icon_checks` が
    `NF_SYMBOLS` 無しのとき 6 件を「ok（未検査）」と報告していたのを
    「skip」にし、`verify.py` も最後に 1 行まとめを出すようにした。
    **マークの置換先が GDEF でもマークかを誰も見ていなかった**
    ——上の U+0310 がそれで出荷されていた。3 つの検査スクリプト
    すべてから見る
  - なおも検査の穴を埋める。**マークの置換先をたどる閉包が合字で
    止まっていた**——Source Code Pro の `ccmp` は結合記号 2 つを 1 つに
    合成する規則を 30 個持ち、出力 22 字のうち 21 字はどの符号位置からも
    届かないので、cmap の走査でも置換の閉包でも見えない。今はクラス 3 が
    ドナー任せで、1 つでもクラスを落とすと `x` + U+0302 + U+0301 が
    1200u から 1800u になる（重ねたアクセントが 600 の独立した字に
    なる）。構成要素が**全部**マークの合字だけをたどる（`Ą` は `A` と
    オゴネクなので、これをマークにすると送り幅が 0 になる）。
    **アクセントの検査が横方向のずれを見ていなかった**——土台アンカー
    502 個を +600 すると `b` の鋭アクセントが次の桁に出るが、
    「字の上に乗っている」は成立するので通っていた。**欧文面と可変
    フォントは `mkmk` と GPOS の `ccmp`、GDEF のマーク結合クラスを
    見ていなかった**（`mkmk` を落とすと 2 つ目のアクセントが 1 つ目に
    重なり、GPOS `ccmp` を落とすと `b͡d` のタイバーが上伸部に乗らなく
    なるが、どちらも通る）。**可変フォントは合字が宣言どおりのセル数か
    見ていなかった**（`==` を 2 セルから 3 セルに広げても通る）ほか、
    `MONA_AMBIGUOUS` の 1 セル、`hhea.advanceWidthMax`、hmtx の
    左サイドベアリングも未検査だった。あわせて、ドナーが本来付く字に
    しかアンカーを持たない結合記号（`U+031A` `U+031B` `U+0334`
    `U+0344`、イタリックでは `U+0310`）が次のセルに描かれることを
    README に書き、その集合が**増えない**ことを検査する
  - 最後に検査の穴を 1 つ。**機能が「効いているか」を見ていたのは JP 面
    だけだった**——欧文静的面と可変フォントはタグが `FeatureList` に
    あるかしか見ておらず、ルックアップの並びを空にした（＝何も置換
    しなくなった）フォントが通る。`ss03`〜`ss08` に至ってはどの面でも
    未検査だった。可変 2 面は `SumiMoji.zip` そのもので、その GSUB は
    マスターを varLib で合成したもの——ほかの面には無い壊れ方をする。
    合字グループ 8 つそれぞれに 1 つずつ、`zero` `cv01` `cv02` `salt`
    `cv99` に 1 つずつ実際に字が変わるかを見る検査を共通化して、
    3 つのスクリプトすべてから呼ぶ（ルックアップを空にした変異
    フォント 5 通りで落ちることを確認済み）
  - 同じ面を 2 回焼くと GSUB のバイト列が変わっていた（`_guard_subtables`
    が合字の並びを集合のまま回していたため、連鎖文脈の規則 16 組が
    毎回別の順に出ていた）。内容は同一で、19 字の演算子アルファベット
    から作った 67,239 通りの文字列で整形結果も完全に一致するので出荷物に
    影響は無いが、ビルドの差分が取れない。順序を固定した
  - 計画書の「4. ビルド構成」が v4 当時のままだった（`dist/latin/term`、
    `SHCJ`、35 ファミリー）ので v5 の構成に書き直し、`build_latin_vf.py`
    `nerdpatch.py` `harmonize_latin.py` `verify_latin_vf.py`
    `verify_many.py` を加えた。Source Han Sans のギリシャの送り幅を
    「602〜795」と書いていたのも実測（基準ウェイトで 285〜804、
    キリルは 454〜1005）に直した——`grid_step` はいちばん近い段に
    寄せるので、いちばん広い字は全角に上がる。1 セルに揃えているのは
    その後に走る `narrow_letters`
  - リリース手順: 週次の上流同期が**必ず失敗するタグを投げていた**。
    最後の `v*` タグのパッチを上げた番号（`v4.0.1`）で `release.yml` を
    起動するが、`CHANGELOG.md` の先頭は未リリースの `## v5.0.0` なので、
    32 面を 15 分かけて組んだ末に最後の手順で落ちる（しかも
    `## v4.0.1` の節があったら v5.0.0 の内容をパッチ番号で出していた）。
    `CHANGELOG.md` の先頭の節がタグより新しければそちらを採り、節が
    無ければリリースを出さない（同期そのものは止めない）——既定の
    パッチ番号の節は誰もまだ書いていないので、節が無いだけで
    落とすと、上流が動いた週にピンの更新ごと失われる。PR の本文に
    「節が無いのでリリースは出さない」と書いて人に渡す。`release.yml` 側にも preflight ジョブを足して、
    ビルドの前に節の中身（見出しだけでなく本文）を見る。
    package ジョブは `harmonize_latin.py` が書き換えた 20 面すべてを
    検査し直す（従来は 2 面だけだった）
- ビルド時間の短縮: Term の全角グリフ約1.7万個は描き直さず charstring の
  中で右へ動かす（`shift_charstring`——先頭の vstem 座標と最初の moveto
  だけを動かし、幅オペランドを付け替える）ので、Source Han Sans 自身の
  ヒントが残り、Term 1 面のヒント付けが 99 秒から 10 秒前後になった
  （品質面でも、自動ヒントより元のヒントが残るほうがよい）。
  `nerdpatch.py` は面ごとの FontForge 実行をコア数分並列に、
  `build_latin_vf.py` は Upright と Italic を並列に組む。CI は可変フォント
  を別ジョブで並行して組む。JP の Regular 6 面はローカル 4 コアで
  2 分（以前は 9 分）。
  さらに、保存のたびに fontTools が外接矩形と hhea / vhea の広がりを
  求め直すために全グリフ（JP 1 面あたり約 1.9 万）を 3 回描き、描いた
  charstring を全部コンパイルし直していたのをやめ、`update_bbox` が
  1 回の走査で head / CFF FontBBox / hhea / vhea を揃えて（値は fontTools
  の再計算と同一）、以後の保存（面の保存、otfautohint、cffsubr、
  `nerdpatch.py`）は再計算なし。走査で解いた charstring の
  バイトコードは戻すので、触っていないグリフは読んだままの形で書き出す。
  otfautohint は子プロセスではなく同一プロセスで呼ぶ（その保存も再計算
  なし）。`=` バーの二分探索は VF をインスタンス化せず
  `getGlyphSet(location=)` で各位置のアウトラインを直接読む（1 プローブ
  0.8〜2.3 秒 → ほぼ 0 秒。丸めなしのバーを見るので、収束位置が以前と
  1 wght 前後ずれ、静的 Sumi Moji のアウトラインが 1〜2u 動く）。
  ローカル 4 コアで、JP Regular 6 面 55 秒（2 分 → ）、Sumi Moji Regular
  6 面 15 秒（64 秒 → ）、VF Upright 21 秒（3 分 → ）、`verify_latin_vf.py`
  61 秒（112 秒 → ）。
  `verify_latin_vf.py` は VF をインスタンス化せず（40 回していた）、
  `getGlyphSet(location=)` のアウトラインと HarfBuzz の variations で
  各位置を検証する（61 秒 → 2 秒）。
  ワークフローはマトリクスに分割（同時に走るジョブは 10 前後が上限で、
  それを超えると待ちが出る）: CI は Regular Upright / Regular Italic /
  Light Italic をドナー別（ship → 基本 + 35 の面、term → Term の面）に
  1 ジョブずつの 6 ジョブ、可変フォントと Sumi Moji Regular への Nerd
  Fonts パッチ（記号セット 1 つ: `SUMI_NERD_SETS`。`--complete` は面の
  大きさによらず 1 面 1 分かかるため、全面・全セットはリリースで）を
  1 ジョブ、の 8 ジョブで 1 分程度（`upstream-sync.yml` の
  `REQUIRED_CHECKS` はこのジョブ名一覧）。リリースはファミリー × ウェイト 2 つ組の 9 ジョブがそれぞれ
  Latin ドナー 4 面・JP 4 面・NF パッチ（Sumi Moji のパッチは JP 面の
  ビルドと並行）まで組んで `verify_many.py` で並列に検証し、`package`
  ジョブが可変フォント 2 本を組み、アーティファクトを集めて Sumi Moji
  （静的・NF）の usWinAscent/Descent をファミリー全体で揃え
  （`harmonize_latin.py`）、VF を静的面と突き合わせ、zip・リリースを
  作る。FontForge は apt ではなく展開済みの AppImage をキャッシュして
  使う（12〜15 秒 → 1 秒）。面フィルタは語の組み合わせになり、
  「Light Upright」「Regular Upright Term」「Light Normal base」のように
  書体（Upright / Italic）・変種・複数ウェイトを絞れる（`build_latin.py`
  では `ship` / `term`）。共通の準備手順は `.github/actions/setup-build`
- Sumi Moji 静的面（とその VF のマスター）の hmtx 左サイドベアリングが
  SCP VF の既定マスター（wght 200）の値のままだった（CFF の lsb は
  fontTools が保守しない。インスタンス化でアウトラインだけ動く）。
  `static_base` がアウトラインから測り直す（`build.sync_lsb`）。
  `verify.py` / `verify_latin.py` が全グリフの lsb と xMin の一致も検査する
- Sumi Moji 静的面のアウトラインを SCP VF の「正確な補間」に揃えた。
  fontTools のインスタンサは charstring の相対オペランドを 1 つずつ丸める
  ため、経路に沿って誤差が溜まり、小数 wght のインスタンスは HarfBuzz が
  VF を描く位置から 3u（'A'）〜数 u ずれていた。静的面は丸めなしで
  インスタンス化してから各点を絶対座標で丸め直す（`round_outlines`）ので、
  VF の描画と 0.5u 以内で一致する（`verify_latin_vf.py` の静的面比較は
  3u → 1u）。JP 各面はこの Latin 面を取り込むので、欧文グリフが旧ビルド
  比で最大 3〜4u 動く（和文グリフは同一）
- Nerd Fonts 変種: FontForge の往復で消えていた元の面のメタデータを
  `nerdpatch.py` が戻す — STAT テーブル（FontForge は書き出さない）、
  post.isFixedPitch / PANOSE の等幅宣言、usWeightClass・fsSelection・
  ベンダー ID・typo / win メトリクス（Sumi Moji は外接矩形全体を覆う方針
  なのでアイコンの分まで広げる。JP 各面は Source Han Code JP の値のまま）。
  `verify_latin.py` は平坦化された NF 面の FDArray 検査を飛ばす。
  外接矩形と hhea / vhea は、元の面から持ち越されたグリフ（FontForge が
  `Identity.N` と名付ける元の `cidN`）は元の面の charstring で測り、
  font-patcher が足した・置き換えたグリフだけ平坦化後の charstring を
  描く（`patched_bounds`。平坦な charstring を全部描くと JP 1 面 15 秒、
  ジョブ内の負荷で 75 秒かかっていた）。
  release.yml は `[release-dry]` と書いたコミットのブランチ push でも
  走り（ビルドと梱包まで、公開なし）、ワークフロー変更の所要時間を main
  に入れる前に測れる。可変フォント 2 本は Term のジョブが組む
- リファクタリング: 7 箇所に複製されていたグリフ追加の前置き
  （`append_context`）、方針の違う 4 箇所の cmap 書き込み（`set_cmap`）、
  `build.py` / `build_latin.py` の `main()` と面の後処理
  （`env_paths` / `run_faces` / `write_face`）、STAT 構築（`add_stat` が
  静的面の 1 値と VF の全値の両方を担当）、SHCJ のバー目標
  （`shcj_bar_target`）、VF 側の wght 探索（`VFSource.matched_wght` /
  `floor_bar` — 静的面と同じ探索になり、VF の名前付きインスタンスが
  静的面と厳密に同じ位置に乗る。実測の位置は Upright 317/374/406/545/
  670/857、Italic 317/377/399/538/661/841 と 1 前後動いた）を共通化。
  name テーブルの nameID 5（Version）も `OWNED_NAME_IDS` に含め、書き換え
  前に旧レコードを全プラットフォーム分消す。`build_latin.py` もフィルタ
  無しの実行では古い面を先に消す。検証・梱包スクリプト共通の
  `scripts/verifylib.py`（HarfBuzz シェイパー、ok/FAIL 集計、ヒント
  検出、静的面の列挙）。未使用の `mona_onecell` と 667 セル前提の
  `MONA_K` 既定値、`fetch-upstreams` の未使用 `cache` 入力を削除。
  面数が 3 以上ならフィルタ付きでもプロセスプールで組む
- ドキュメント: CONTRIBUTING が旧い 1 段階ビルドを説明していたのを 2 段階
  に更新。README / CHANGELOG の「Sumi Moji に TTC・NF は無い」を訂正
  （リリースは `SumiMoji.ttc` / `SumiMoji-NerdFont.zip` を添付済み）

### v3.3.0 として準備していた変更（未公開、v4.0.0 に含む）

- `scripts/build_latin_vf.py` を追加: Sumi Moji を CFF2 可変フォントとして
  組む（`dist/latin/SumiMoji[wght].otf` / `SumiMoji-Italic[wght].otf`）。
  マスターは SCP VF 自身のマスター位置（wght 200 / 400 / 900、CFF2 の
  VarStore から実測——決め打ちしない）にそのまま置き、各マスターで
  Monaspace を太さ一致でインスタンス化するが、erosion（Monaspace の
  wght 下限対策）は非線形なブーリアン演算でマスター間の補間に使えない
  ため無効化し（`VFSource.matched(erode=False)`、下限で単にクランプ）、
  重なり除去（`pathops.simplify`、`draw_clean(simplify=False)`）も
  マスター間の点対応を崩すため行わない——重なりは SCP 自身の VF と同じ
  扱いで残す。ヒント付け・サブルーチン化もしない。fvar の6つの名前付き
  インスタンス（Light/Normal/Regular/Medium/Bold/Heavy）と STAT の値は
  静的版の STAT と同じ usWeightClass の値（300/350/400/500/700/900、
  既定 400 = Regular で OS/2 usWeightClass と一致——軸を指定せずに VF を
  選んでも Regular が出る）。avar が各値を静的版と同じ「SHCJ の `=`
  バー×600/667」に一致する SCP wght（実測: Upright で 317/374/406/546/
  669/857、Italic で 317/378/399/538/662/841）へ写す。マスターを置く
  設計座標は SCP のユーザー wght を SCP 自身の fvar 正規化 + avar で
  線形化したもの（SCP の VF はユーザー wght に対して線形ではない）で、
  SCP 自身の avar の折れ点も写像に含めるため、名前付きインスタンスの
  間でも SCP と一致する。マスターは fontTools の instancer の整数丸めを
  切ってインスタンス化する（`unrounded_cff2_instancing`）——charstring の
  オペランドは相対座標なので丸めが経路に沿って累積し、丸めたマスターから
  組むと `m` などがマスター間で最大 10u ずれていた（実測）。丸めなしなら
  SCP のブレンドそのもの（CFF2 charstring の固定小数 16.16 精度）を補間
  するので、`verify_latin_vf.py` は名前付きインスタンスの間の wght でも
  SCP 自身のブレンドと 1u 以内で一致することを確認する。代償として
  VF のファイルサイズは整数版の約 4.5 倍（Upright 約 1.6MB——
  オペランドが 16.16 固定小数 5 バイトになるため）。マスターは SCP の 200 / 400 に
  Regular と Heavy の位置（Regular が既定マスター、Heavy が軸の上限）と
  Monaspace の下限位置（Monaspace wght 200 のバーが SCP のバーと一致する
  SCP wght——これより細い側は Monaspace が下限でクランプされ一定、太い側
  は SCP 追随）を加えた 5 つ。name テーブルは SCP VF 自身の慣習
  （`SourceCodeVF-Upright.otf` / `-Italic.otf`）に倣い、nameID 6 に
  `SumiMoji-Roman` / `SumiMoji-Italic`、nameID 25 に `SumiMoji`
  （バリエーション PostScript 名接頭辞）、nameID 16/17 は省略（fvar +
  STAT が既に家族を説明するため）。軸の既定値に置かれた名前付き
  インスタンス（Regular / Italic）の postScriptNameID は fvar の仕様
  どおり nameID 6 を指す（fontbakery
  `opentype/varfont/valid_default_instance_nameids`）。MVAR/HVAR は除外（このレシピでは
  送り幅もOS/2の縦メトリクスも太さで変化しないため、可変にする対象が
  無い）。SCP wght がおよそ366を下回ると Monaspace 側の記号・合字は
  自身の wght 200 の下限（静的版が erosion で削っている太さ）より
  薄くできないため、VF の軽量側では記号だけが下限の太さで止まり、
  erosion 済みの静的 Light よりわずかに太くなる——静的 Light は引き続き
  erosion 版を配布する。`scripts/verify_latin_vf.py` を追加（fvar/STAT/
  name の形状、6つの名前付きインスタンス全てでの合字シェイピング、
  6 つの名前付きインスタンスそれぞれの `=` バーと `A` の外接矩形を対応する
  静的面と比較、`SCP_VF_U` / `SCP_VF_I` がある環境では名前付き
  インスタンスの間の wght でも SCP 自身のインスタンスと外形が一致する
  ことを確認）。CI（`ci.yml`）は `build_latin.py` の Regular 検証の
  直後に Upright VF のビルドと検証を追加（時間短縮のため Upright のみ）、
  リリース（`release.yml`）は Upright・Italic 両方をビルド・検証し、
  `SumiMoji.zip` にも自動的に含まれる（zip 手順は `dist/latin` 内の
  `*.otf` を素朴に glob しているため）

- name テーブルから nameID 7（商標: "Source is a trademark of Adobe"）と、
  静的面では nameID 25（バリエーション PostScript 名接頭辞、SCP 由来の
  `SourceCodeUpright` / `SourceCodeItalic` が残っていた）を落とす
  （`build.OWNED_NAME_IDS`）——どちらも Source という名前のフォントの
  ためのもので、Adobe の表示は nameID 0 のクレジットに入っている。
  Sumi Moji（静的・VF とも）では SCP が GDEF のマーク分類を付けていない
  結合文字 U+035F / U+0361 も class 3（Mark）にする
  （`build.classify_unicode_marks`）
- 欧文のみの新ファミリー Sumi Moji（仮称、PostScript 名 `SumiMoji-*`）を
  追加し、`build.py` はこれを Source Han Sans に接ぎ木する側に変更
  （VF に直接触らなくなった）。`scripts/build_latin.py` が VF から直接
  組む: Source Han Code JP の `=` バーに合わせた Source Code Pro VF の
  インスタンスを fontTools の CFF2ToCFF で静的 CID-keyed CFF に変換した
  ものをベースに（SCP のアウトライン・アライメントゾーン・GSUB
  （`cv01`〜`cv17` `zero` `salt`、SCP の stylistic set は `ss11`〜`ss17`
  に移動）・GPOS のマーク位置決めはそのまま生存）、インスタンス化で
  失われるヒントを SCP 自身のゾーンに対して otfautohint で付け直し、
  Monaspace の合字61種・ASCII 記号32字・1セル矢印（SCP に無い `⇔` も
  追加）を接ぎ木し、cffsubr でサブルーチン化する。プロファイルは2つ:
  配布物 `dist/latin`（バー = SHCJ のバー × 600/667、35 と同じ太さ）と
  内部専用 `dist/latin/term`（バー = SHCJ のバー を 600 のまま、Term
  ファミリー用ドナー）。結合文字は SCP が出荷する形（スペーシング、
  GPOS mark で位置決め）のまま。縦メトリクスは SCP 自身の hhea
  （984/-273）、typo を hhea と同値にして `USE_TYPO_METRICS` を立て、
  win はファミリー全面のバウンディングボックス（Regular ペアで
  1060/454）。Regular は1,632グリフ/約140KB、Italic は1,335グリフ
  （SCP Italic VF のグリフ数がそもそも少ない）。CI（`ci.yml`）は
  `build_latin.py` を先に走らせてから Regular ペアをビルド・検証、
  リリース（`release.yml`）も同じ順序で12面をビルドし2面を検証した
  うえで `SumiMoji.zip`（LICENSE 同梱）をリリース資産に追加。JP 側の
  出力は roundoff（±1〜2ユニット）を除き従来の VF 直接ビルドと同一に
  なるよう意図しており、`scripts/golden.py` が2つの dist ディレクトリ
  間で cmap・送り幅・シェーピング・アウトライン（許容誤差つき）・
  メタデータ・ヒントを比較する。`makeotc.py` / `nerdpatch.py` も
  Sumi Moji を扱い、リリースは `SumiMoji.ttc` と `SumiMoji-NerdFont.zip`
  も添付する
- 合字を50種から61種に拡張。Monaspace が描いているが `data/mona_ligs.json`
  が未収録だった11種を追加: 真の合字5つ `!~` `=~`（正規表現マッチ、ss01）、
  `~~>`（ss02）、`<!--`（ss03、4セル）、`&&=`（ss08）と、`::`/`:=` と同じ
  手法で合成する文脈的スペーシング代替6つ `&&` `++`（`&`/`+` の
  init/fina 変異、ss08）、`..<` `.=`（ピリオドを上げた変異、ss06）、
  `:>` `<:`（コロンを上げた変異、ss05）。結果として `&&=` と `~~>` は
  合字化するようになった（Monaspace 本家と同じ挙動）。文脈ガードは
  `<|>` `->>` `==>` を引き続き素の文字のまま保つ
- JP / 35 の全角矢印7種（`←` `→` `↑` `↓` `⇐` `⇒` `⇔`）を Monaspace から
  取り直し。合字グリフ（`->` `<-` `=>` `<=>`、`⇐` は `=>` の鏡像、
  `↑` `↓` は `->` の 90° 回転）の軸を詰めるか伸ばすかして Source Han
  Sans のインク長に合わせる（Italic は傾斜を抜いてから加工して後で
  掛け直す）。全角の幅・インク量は SHCJ のまま、矢尻とストロークが
  合字と同一になる。Monaspace 自身の `→` 文字は `->` より矢尻が小さい
  ので使わない。`≠` `≤` `≥` `…` は軸がないため引き続き Source Han Sans
  の全角グリフ
- 合字と対になる11字（矢印7種 + `≠` `≤` `≥` `…`）に、全ファミリーで
  全角・半角（Monaspace 1セル）両方の字形を用意し OpenType feature で
  手動切り替え可能に: JP / 35 は既定が全角で `hwid` / 新設の `ss09`
  （「Half-width arrows & operators」、この11字だけを動かす）が半角、
  Term は既定が半角（従来どおり）で `fwid` が全角に戻す。フォントは
  ターミナルの曖昧幅設定を検知できないための手動対応。HackGen Console /
  PlemolJP Console / Moralerspace HW / UDEV Gothic JPDOC は別ファミリーで
  この使い分けを提供しているが、本フォントは各ファミリー内の feature
  として持たせている
- 単独の ASCII 記号は32字全部（`!"#$%&'()*+,-./:;<=>?@[\]^_`{|}~`）を
  Monaspace に統一（以前は `=` `<` `>` `|` `~` の5字のみ）。合字自体が
  Monaspace 製のため、記号の骨格が単独字と合字とで食い違うと隣接時に
  継ぎ目が見えていた（`#` と `#[`、`-` と `->` など）。多くは Monaspace の
  cap 高・x-height が高いぶんの縦サイズ差（16〜150u、セル内に収まり
  ターミナルサイズでは2ピクセル未満）。`-` は `=` より124u短いが
  Monaspace 自身の字形どおり。SCP の cv14/cv15/cv16 を有効にすると
  `-` `*` `$` は SCP の字形に戻る
- 合字の calt/liga: 結合ルックアップの一致条件を、先頭1文字だけを見て
  残りをネストした LigatureSubst に委ねる形から、各合字の入力列を全部
  カバーするトリガールール（最長一致順）に変更。ネストしたルックアップが
  一致範囲を超えて何文字消費してよいかは OpenType の未定義動作で、
  HarfBuzz（kitty / WezTerm / Ghostty / VS Code のエディタ本体）は許容
  していたが DirectWrite（Windows Terminal）は v3.2.0 で `->` を素の文字
  のまま描画していた。Monaspace 本家の calt も入力を全部カバーする形。
  なお VS Code の統合ターミナル（xterm.js のリガチャアドオン、
  font-ligatures）は Fira Code 式の単純な置換ルールしか解釈せず、
  このフォントの合字はどのバージョンでも表示されない — これは今回の
  変更と無関係で従来通り
- 等幅メタデータ: `post.isFixedPitch=1`、PANOSE bProportion=9
  （HackGen / PlemolJP と同じ）、xAvgCharWidth を OS/2 v3+ の定義
  （0でない送り幅全部の平均）で再計算 — SHCJ の値の複写をやめた。
  Windows Terminal のフォント選択に「すべてのフォントを表示」なしで
  出るようになる
- OS/2 sxHeight / sCapHeight を各面の実際の `x` / `H` から実測。
  SHS の値（543/733）は Term/35（SCP 原寸で 488/655）に対して誤りだった
- ヒンティング: T2CharStringPen で描いた欧文・合字・（Term では）拡幅した
  全角グリフはこのフォント最初のバージョンから CFF ヒントを持っていな
  かった（SHCJ の欧文にはヒントがあった）。最終アウトラインで測った
  アライメントゾーン（BlueValues/OtherBlues/StdHW/StdVW）付きの CID
  FontDict を新設して割り当て、面を保存した後に AFDKO の otfautohint で
  ヒントを付ける（`requirements.txt` に afdko 追加、
  `SHOYU_SKIP_AUTOHINT=1` でローカルビルド時はスキップ可）。Source Han
  Sans 由来のグリフは元のヒントのまま。ヒント付与後、cffsubr（AFDKO の
  tx、`requirements.txt` に追加）で CFF をサブルーチン化: ビルドが生成
  する charstring はすべてフラットで、Term は全角グリフ約1.7万個を
  丸ごと再生成するため、ヒント付きだと Term の面が44%肥大（6.7MB）して
  いた。サブルーチン化後は4.6MB — ヒントを保ったまま v3.2.0（4.66MB）より
  小さい
- GDEF: SCP から移植した結合文字（U+0300〜036F、送り0）を class 3
  （Mark）に分類
- 来歴: nameID 0 にプロジェクトの著作権表示に加え Source Han Sans /
  Source Code Pro / Monaspace 各ドナーの表示をビルド時に取り込んで記載、
  nameID 9 にドナーのデザイナーを列挙、nameID 8 / 11 が Adobe ではなく
  プロジェクト（hn-11、リポジトリ URL）を指すように変更、
  OS/2 achVendID を Adobe の `ADBO` から未登録の `SHYU` に、
  nameID 3（unique ID）を `version;SHYU;PostScriptName` 形式に。
  Source Han Sans の古い DSIG は削除
- STAT テーブルを追加。各面は自分自身の wght / ital 値を1つずつだけ
  持つ（Regular は elidable で Bold にリンク、upright は elidable で
  Italic にリンク） — ファミリー全体の値を全面に列挙する形は Windows の
  ファミリーモデルを混乱させる（fontbakery: STAT_in_statics）ため
  避けた。OS/2 に WWS ビットを立て（version 4 に更新）、usWeightClass も
  ウェイトごとに明示的に設定

## v3.2.0

- 合字に文脈ガードを追加（Monaspace 本家の calt と同じ考え方）。演算子の並びが
  どの合字よりも長いときは合字化しない: `<|>` `&&=` `~~>` `->>` `==>` は
  素の文字のまま、`->` `-->` `<-->` `>>=` `===` は従来どおり合字。

## v3.1.0

- Light ウェイト復活（Monaspace 下限を超える分をアウトライン削りで合わせる）
- リリース版数を name ID 5 / fontRevision に刻印
- SCP の未収録約 600 字（`ł` `ğ` `ş` `ı` `ř` `₽` …）を半角で取り込み
- `−` `ς` `⁴` などのグリッド外グリフ修正
- 35/Term で半角カナを 1 セルに中央配置
- Term/35: `pwid`/`palt` を外し `hwid` 先のグリフをセルに合わせる
- NF 変種のアイコンを JP/35 でも 1 セル幅に
- 結合文字（U+0300〜036F）を送り 0 で取り込み、直前の文字に重なるように
- OS/2 の Unicode / コードページ範囲ビットを最終的な収録文字から再計算
- `makeotc.py` がファミリー内で cmap が一致しない面を検出して止まるように
- リリース zip に LICENSE 同梱

## v3.0.0

破壊的変更を含みます。

- ExtraLight/Light 削除（Light は 3.1.0 で復活）
- `= < > | ~` を Monaspace に
- Term の曖昧幅を HackGen Console 方式に変更

**既存ユーザーへの影響**: Term で `①` `※` が 2 セル幅になり、曖昧幅を
半角扱いするターミナルでは右隣に食み出す。Windows Terminal は
`"compatibility.ambiguousWidth": "wide"` で 2 セル確保できる。`←` `→` `≠`
`≤` `…` は 1 セル（Monaspace 版）。

- liga 単独で合字が出なかったバグ修正
- Term が CI で検証されるように
